# Changelog

## Unreleased

- README: add "Pairs with RTK" — input/output token economy framing
- README: add "Why a proxy" framing — caching, policy, cross-vendor as roadmap
- feat: graceful restart via `SIGUSR1` re-exec + `make deploy` target
- feat: tool result breakdown by tool name in `history.jsonl`
- docs: add forums read-on-start convention
- docs: move `CACHE.md` from textworkspace (where it didn't belong)

## v0.2.0

- feat: `version.go` + `stats --json` + tw compatibility

## v0.1.4

- chore: bump (history before this point is not reconstructed; see git log for detail)
